# Tripsphere Proto Package

This package contains the generated protobuf Python code for the Tripsphere project.

## Generation

The proto files are automatically compiled during the Docker build process.

