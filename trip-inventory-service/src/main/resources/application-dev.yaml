spring:
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: public
        group: DEFAULT_GROUP
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory_db
    username: postgres
    password: fudanse
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379

grpc:
  server:
    port: 50061
    enable-reflection: true

logging:
  level:
    org.tripsphere.inventory: DEBUG
