# https://taskfile.dev

version: '3'

tasks:
  default:
    desc: List all available tasks
    cmds:
      - task --list-all
    silent: true

  ruff:
    desc: Run ruff format and ruff check --fix
    cmds:
      - uvx ruff format
      - uvx ruff check --fix