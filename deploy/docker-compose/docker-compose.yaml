# Deployment entrypoint for the canonical local stack.
# Requires Docker Compose 2.20.0 or newer for top-level `include` support.
name: tripsphere

include:
  - path: ../../docker-compose.yaml
    project_directory: ../..
