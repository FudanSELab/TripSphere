spring:
  application:
    name: trip-product-service
  profiles:
    active: dev

server:
  port: 24220

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    org.tripsphere.product: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
