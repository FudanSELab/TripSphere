[project]
name = "tripsphere"
version = "0.1.0"
readme = "README.md"
dependencies = [
    "grpcio>=1.75.1",
    "protobuf>=6.32.1",
]

[build-system]
requires = ["uv_build>=0.8.19,<0.10.0"]
build-backend = "uv_build"
